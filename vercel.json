{
  "framework": "vite",
  "installCommand": "npm ci --workspaces --include-workspace-root --no-audit --no-fund --ignore-scripts",
  "buildCommand": "npm run build",
  "outputDirectory": "apps/web/dist"
}
