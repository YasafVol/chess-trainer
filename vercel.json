{
  "framework": "vite",
  "installCommand": "pnpm install --ignore-scripts",
  "buildCommand": "pnpm --dir apps/web exec convex deploy --cmd 'pnpm run build'",
  "outputDirectory": "apps/web/dist"
}
