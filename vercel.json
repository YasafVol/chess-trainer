{
  "framework": "vite",
  "installCommand": "cd apps/web && npm ci --no-audit --no-fund --ignore-scripts",
  "buildCommand": "cd apps/web && npx convex deploy --cmd 'npm run build'",
  "outputDirectory": "apps/web/dist"
}
